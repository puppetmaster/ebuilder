%h1 Project :
%table{:border => "1"}
  - @project.attributes.each_pair do |atr,val|
    - if "#{atr}" != "id"
      %tr
        %th= atr.capitalize
        %td= val
%br
%a{:href => "/project/edit/#{@project.id}"} Edit
%a{:href => "/project/delete/#{@project.id}"} Delete
